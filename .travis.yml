language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
script:
  - pytest tests/  --doctest-modules --pep8 coveralls -v --cov coveralls --cov-report term-missing
after_success:
  - coveralls